# TR大阪 AIツール 不具合ログ
最終更新：2026年8月20日（Claude Code対応分を反映）

---

## 記録ルール
- 発生した不具合・気になる挙動をここに記録する
- 原因不明のものも「調査中」として記録
- 解決したら「解決済み」に移動し、原因と対処法を記録する
- 2026年7月8日〜：Notion「🪵 AIツール_ログ」DBに実際のエラーが自動記録されるようになったため、今後はそちらを見れば実際に起きているエラーの内容（発生頻度・APIの生エラーメッセージ等）を確認できる

---

## 未解決・調査中

（現時点でなし。BUG-001はログでの原因特定・自動リトライ実装により対応済み。下記FIXED-011参照）

---

## 解決済み

### FIXED-020：実績を調べるタブで「夏向け冷感グッズ」等のテーマ検索が0件になる
- **発生タブ**：④実績を調べる
- **症状**：「業界・テーマ・条件」欄に自由文を入れて検索すると、属性タグ的には該当商品があるはずなのに0件になる
- **原因**：①アイテム提案タブにはテーマ文を「検索ワード変換辞書」で属性タグに変換するロジックを実装していたが、④実績を調べるタブには入れ忘れていた（商品名の文字列完全一致検索のみだったため、「冷感グッズ」等の言葉が実際の商品名に含まれず0件になっていた）
- **対処**（2026年8月19日）：④にも同じ辞書変換ロジックを追加。あわせて、辞書変換に成功した場合はキーワード文字列検索とのAND条件になって0件になりやすいため、変換成功時はタグの絞り込みのみを使うよう調整

### FIXED-019：PPTX生成した資料の英数字部分が文字化けする
- **発生箇所**：①④⑤すべての資料作成（パワポ生成）
- **症状**：生成したPPTXをPowerPointで開くと、日本語（漢字・ひらがな・カタカナ）は正常なのに、「Item Lineup」「Confidential」「01」や日付の数字など英数字部分だけ意味不明な記号列に化ける
- **原因**：2つ複合していた
  1. テンプレートPPTX（Google Drive格納のFMTファイル）のスライドXMLに、東アジア/複合文字用フォントのcharset属性が不正な負数（例：`charset="-122"`）で埋め込まれている箇所が208件あった（OOXML仕様上charsetは0〜255の符号なしバイト値のはずで、負数は本来ありえない値。テンプレート作成時に使ったツールの不具合と思われる）
  2. さらに、フォント指定「Noto Sans JP」が、沖さんの環境では半角英数字のグリフ（字形）を正しく描画できなかった（漢字は正常描画されたため、フォントファイル自体の不完全さか、Windows側のフォントリンク設定の問題と推測）
- **対処**（2026年8月19日）：パワポ生成の共通関数`modifyAndDownloadPptx`内に、生成のたびにテンプレート由来の全スライド／ノートXMLから不正なcharset属性を除去し、フォントをWindows/Office標準の「Yu Gothic」に差し替える処理（`sanitizeInvalidCharsetAttrs`）を追加。全資料作成タブ共通の処理のため、個別対応不要

### FIXED-018：属性タグ分類の見直しで「推し活」を廃止・タグ体系を大幅変更
- **発生箇所**：③属性タグ機能（Notion「属性タグ判定キーワード辞書」）
- **経緯**：属性タグ初期実装時は16分類だったが、沖さんが実際の辞書を見て「推し活」という広すぎる1分類を廃止し、より具体的な複数分類（アクリル・キーホルダー・チャーム・缶バッジ・ぬい・クッション・ペンライト・うちわ・ボイス・マグネット等）に分割。その後さらにインテリア・おもちゃ・食品・アクセサリーの4分類を追加し、最終的に28分類（うち「推し活」はオプションとしては残っているが未使用）になった
- **対応**：タグ体系を変更するたびに、①Notion側3箇所（決定商品DB・裏取りDBの属性タグ選択肢、検索ワード変換辞書の対応属性タグ選択肢）と②worker.jsの`ATTRIBUTE_TAGS`定数を同期し、③該当商品の再判定を実施。当初は全件を洗い直す`/attribute-tag/reset-all`のみだったが、都度全件処理し直すのは非効率なため、空欄の商品だけを再判定キューに戻す軽量版`/attribute-tag/reset-empty`を新設し、以後の辞書追加時はこちらを使う運用に変更
- **教訓**：Notionの選択肢（multi_select）は沖さん側でいつでも追加・変更できるため、辞書の内容は都度実際にNotionを確認してから作業すること。決め打ちで進めない

### FIXED-017：業界タグ一括処理でNotion書き込みが断続的に失敗する
- **発生箇所**：業界タグのマスタ同期（`/industry-tag/master-sync`）・バックフィル（`/industry-tag/backfill-apply`）
- **症状**：1回のWorker呼び出しで大量（100件超）のNotion書き込みを連続実行すると、一部が失敗する（レスポンスの`failed`件数が数十件になる）
- **原因**：Notion APIのレート制限（429）に断続的に引っかかっていた。当初はリトライ処理がなかった
- **対処**（2026年8月14日・Claude Code）：429応答時に`Retry-After`ヘッダーを見て待機・再試行する共通関数`fetchNotionWithRetry`を追加。あわせて未処理のまま残った行は次回バッチで自動的に再試行される設計にしたため、複数回実行すれば最終的に全件処理される
- **備考**：1回の呼び出しあたりの件数を50〜80件程度に抑えるとより安定する

### FIXED-016：業界タグ対応表CSVの手動転記でデータ品質問題が発生
- **発生箇所**：取引先業界タグ付けDBへのCSV初期投入
- **症状**：Notionへの一括投入をAIが手作業でJSON転記した際、似た形の漢字への誤変換（例：摂南大学→攝南大学）、存在しない行の誤挿入、同一社名の重複登録などが発生
- **原因**：MCP経由のNotion一括作成ツールがファイル参照に対応しておらず、内容をその都度手打ちで転記する必要があったため
- **対処**（2026年8月14日）：発覚した誤りは個別に修正・重複は無効化（該当ページの業界タグを空にして突合対象から除外）。以後の同様の一括投入は、Worker側に専用APIエンドポイントを作り、ファイル内容をそのままAPIに渡す方式に変更してヒューマンエラーの余地を無くした
- **備考**：今後大量データをNotionに投入する場合、AIによる手動転記ではなく、この方式（Workerに変換ロジックを実装し、ファイルをそのままPOSTする）を踏襲すること

### FIXED-015：CSVのマスタデータに会社形態・単語だけの不完全な行が含まれ、無関係な取引先が誤って一致する
- **発生箇所**：業界タグの突合ロジック（決定商品DBの取引先名とマスタDBの部分一致）
- **症状**：マスタCSVに「合同会社」「有限会社」「Project」のような、社名として不完全な行が混ざっており、これを含む別の会社名（例：「Antree tokyo 合同会社」）が誤って一致してしまう
- **原因**：元データ（SF取引先マスタ）の欠損・簡略化された行がCSVにそのまま含まれていた
- **対処**（2026年8月14日）：突合ロジック側で、法人格トークン（株式会社・有限会社・合同会社等）を除いた「核となる名称」が短すぎる（3文字未満）マスタ行は自動的に除外するようにした

### FIXED-014：仕入先DBの検索が常に0件になる
- **発生タブ**：一問一答・仕入先検索
- **症状**：仕入先DB追加後、明らかに情報があるはずの商品（例：クリアファイル）で検索しても「仕入先DBに情報なし」という回答になる
- **原因**：検索条件に指定していたプロパティ名「仕入先: 仕入先名」が、CSVエクスポート時にのみ付加される表記で、実際のNotion上のプロパティ名は「仕入先名」だった（Notion APIがプロパティ不一致でエラーを返し、結果的に0件扱いになっていた）
- **対処**（2026年8月3日・Claude Code）：Worker・フロントエンド両方のプロパティ参照を「仕入先名」に修正

### FIXED-013b：回答が末尾までスクロールされ、先頭から読むために毎回上に戻る必要がある
- **発生タブ**：一問一答・仕入先検索
- **症状**：質問すると回答文の一番下までスクロールされた状態で表示され、頭から読むには毎回手動で上に戻る必要があった
- **対処**（2026年8月3日・Claude Code）：回答表示後、回答の先頭が見える位置でスクロールが止まるように修正

### FIXED-013：仕入先候補にデータ元・コスト表有無が表示されない
- **発生タブ**：仕入先検索
- **症状**：候補が出ても裏取りDB／仕入先DBどちらから来た情報か分からず、コスト表の有無も表示されない
- **原因**：Geminiへの回答フォーマット指示にデータ元・コスト表有無の明記ルールが含まれていなかった
- **対処**（2026年8月3日・Claude Code）：候補リストに「（裏取りDB）」「（仕入先DB／コスト表あり）」のようにデータ元とコスト表有無を明記するよう回答フォーマットを修正。あわせて、連絡先共有フォーマットも指定の形式（仕入先名／担当者・メール・電話①②を取り違えない／備考）に修正

### FIXED-012：追加質問で仕入先の詳細情報（コスト表格納先・連絡先）が参照できなくなる
- **発生タブ**：一問一答・仕入先検索
- **症状**：「◯◯のコスト表はどこ？」「◯◯の連絡先は？」のように、直前に紹介された仕入先について詳しく聞くと「情報がありません」と返される
- **原因**：
  - 一問一答：追加質問のたびにその質問文からキーワードを再抽出してNotionを再検索する設計だったため、質問文に会社名がうまく含まれない場合、直前の検索結果が失われていた
  - 仕入先検索：仕入先DBの検索は初回検索時にしか行っておらず、「もっと見せて」や2回目以降の質問では裏取りDBのデータしかAIに渡していなかった
- **対処**（2026年8月3日・Claude Code）：
  - 一問一答：会話中に見つかったNotion情報を蓄積・保持し、再検索がヒットしなくても以前の情報を参照し続けられるように変更
  - 仕入先検索：仕入先DBの検索結果を初回検索時に保持し、以降の全ターンでコンテキストに含めるように変更

### FIXED-011：たまに「回答を取得できませんでした」になる（BUG-001）
- **発生タブ**：全タブ（アイテム提案・仕入先検索・一問一答・実績を調べる）
- **症状**：検索ボタンを押すと「回答を取得できませんでした」が表示される
- **原因**：ログ導入（2026年7月8日）で判明。Gemini APIが混雑時に返す`503 UNAVAILABLE`エラーが原因で、キーワードの抽象度などとは無関係だった
- **対処**（2026年7月9日・Claude Code）：Worker側に共通関数`callGemini`を追加し、503/429エラー時に最大2回まで自動リトライするようにした
- **備考**：まれに2回のリトライでも失敗するケースは残りうるが、ログで発生状況を継続的に確認できる

### FIXED-010：実績を調べるタブでチェックボックスの価格が常に「要確認」になる
- **発生箇所**：実績を調べる → 検索結果のチェックボックス欄
- **症状**：数量横の価格表示が全アイテムで「要確認」になる（アイテム提案タブでは正常表示）
- **原因**：決定商品DBのデータをテキスト化する処理（`pageToText`）が「仕入単価」フィールドを含めておらず、価格抽出処理が仕入単価を見つけられなかった
- **対処**（2026年7月9日・Claude Code）：仕入単価をテキスト出力に追加。表示ラベルも「資料表記◯◯円程度」に変更（仕入単価に35%の粗利を乗せて計算、アイテム提案タブと同じ式）

### FIXED-009：資料作成のページ増殖が画像メイン形式では効かない
- **発生箇所**：資料作成 → 画像メイン選択 → 7アイテム以上選択
- **症状**：8アイテム選択しても1ページ（6アイテム分）しか出力されない
- **原因**：スライド増殖処理（FIXED-006）が詳細紹介形式のみに実装されており、画像メイン形式には未対応だった
- **対処**（2026年7月9日・Claude Code）：ページ増殖処理を`generateContentPages`関数に共通化し、画像メイン・詳細紹介どちらの形式でも動作するように変更

### FIXED-008b：ノート欄の参考元情報が3行までしか入らない・複数アイテムが1行にまとまる
- **発生箇所**：資料作成 → ノート欄
- **症状**：ページに4アイテム以上載る場合、3行目以降がまとめて1行に詰め込まれ改行されない
- **原因**：ノート欄のプレースホルダー「・」が3つ固定で用意されており、それ以上は1行に連結する実装だった
- **対処**（2026年7月9日・Claude Code）：ノート欄の「・」プレースホルダーをアイテム数ぶん動的に複製する方式に変更し、1アイテム1行になるようにした。記載内容も「（データ元DB／数量／仕入単価／納期／仕入先）」に変更

### FIXED-008a：商品画像が枠に引き伸ばされて縦横比が歪む
- **発生箇所**：資料作成 → 商品画像（2026年7月9日導入の新機能）
- **症状**：画像がプレースホルダー枠のサイズに強制的に引き伸ばされ、縦横比が崩れて表示される
- **原因**：画像をシェイプの塗りつぶし（blipFill＋stretch）として枠いっぱいに敷き詰めていたため
- **対処**（2026年7月9日・Claude Code）：画像の実際のピクセルサイズを取得し、枠の中に収まる最大サイズ・中央配置になるよう計算して`<p:pic>`要素として重ねる方式に変更。あわせて、余白部分の水色が目立つため背景色を白に変更

### FIXED-006：詳細紹介（2枚版）でスライドが1枚しか出ない（BUG-002）
- **症状**：資料作成 → 詳細紹介選択 → 3アイテム以上選択すると、4アイテム選択しても1枚（2アイテム分）しか出力されなかった
- **原因**：スライド増殖処理が未実装。presentation.xmlのスライド参照リスト（`<p:sldIdLst>`）更新が必要だった
- **対処**（2026年7月6日・Claude Code）：`modifyAndDownloadPptx`内で2アイテムごとにslide3.xmlを複製（slide3_p2.xml, slide3_p3.xml...）し、`registerNewSlides`関数でpresentation.xml・presentation.xml.rels・[Content_Types].xmlに新規スライドを登録するように実装
- **検証**：Node.js + JSZipでのオフラインテストで、5アイテム→3ページ（2,2,1）の分割・sldIdLstの順序・XMLの整形性を確認済み

### FIXED-007：詳細紹介選択時に画像メイン用スライドが残る（BUG-003）
- **症状**：詳細紹介（2枚/ページ）を選択した際、出力pptxに画像メイン（6枚/ページ）用スライドが未置換のプレースホルダーのまま残っていた（逆に画像メイン選択時も詳細紹介用スライドが残っていた）
- **原因**：不要スライド削除処理が未実装
- **対処**（2026年7月6日・Claude Code）：`unregisterSlide`関数を追加し、選択されなかった方のスライド（画像メイン⇔詳細紹介）をpresentation.xml・rels・Content_Types・物理ファイルから完全に除去するように実装

### FIXED-008：ノート欄に参考元情報が記載されない（BUG-004）
- **症状**：生成されたpptxのノート欄が空のまま（参考元情報が入らない）
- **原因**：ノート欄への書き込み処理（`addNote`関数）が定義されていたが、どこからも呼び出されていなかった
- **対処**（2026年7月6日・Claude Code）：`fillNoteBullets`関数を新規実装し、各コンテンツスライド（slide2/slide3およびその複製ページ）のノート欄にある「・」プレースホルダー（3枠）に、そのページに掲載したアイテムの商品名・データ元（決定商品DB/裏取りDB）・仕入先を埋め込むように実装。未使用だった`addNote`・`replaceContactInfo`・`replaceText`・`replaceShapeText`関数は削除（デッドコード）

### FIXED-001：buildCheckList is not defined エラー
- **症状**：アイテム提案で検索すると「buildCheckList is not defined」エラー
- **原因**：関数が1つ目のscriptブロックに定義されており、2つ目のscriptブロックから呼び出せなかった
- **対処**：関数を2つ目のscriptブロックに移動

### FIXED-002：パワポファイルが壊れて開けない
- **症状**：生成されたpptxをパワポで開くと「読み込めません」エラー
- **原因**：JSZipでスライドを削除・追加する際にpresentation.xmlを更新していなかった
- **対処**：スライドの削除・追加処理を一時中止し、テキスト置換のみの処理に変更

### FIXED-003：正規表現エラーで全関数が未定義になる
- **症状**：ボタンを押しても何も起きない。Consoleに「Invalid regular expression flags」
- **原因**：JSの正規表現内で`</a:t>`の`/`をエスケープしていなかった（`<\/a:t>`が必要）
- **対処**：全ての正規表現内の`</a:t>`を`<\/a:t>`に修正
- **教訓**：JSの正規表現リテラル`/.../`の中でフォワードスラッシュ`/`を使う場合は必ず`\/`とエスケープする

### FIXED-004：関数が重複定義されていた
- **症状**：古いバージョンの関数が呼ばれて新しい修正が反映されない
- **原因**：修正のたびに新しい関数を追加したが古い関数を削除していなかったため重複が発生
- **対処**：重複関数を削除
- **教訓**：関数を修正する際は必ず古い定義を削除してから新しいものを追加する

### FIXED-005：FMT Base64デコードエラー
- **症状**：「Failed to execute 'atob' on 'Window': The string to be decoded is not correctly encoded」
- **原因**：Base64文字列に余分な空白が含まれていた
- **対処**：`atob(fmtB64.replace(/\s/g, ''))`で空白を除去してからデコード
